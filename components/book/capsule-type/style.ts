import { StyleSheet } from "react-native"; 

export const styles = StyleSheet.create({
    container:{
        marginVertical: 20
    },
    capsuleTypeButtonsContainer: {
        display:'flex',
        flexDirection:"row",
        width:'100%',
        justifyContent: "space-around"
    },
})