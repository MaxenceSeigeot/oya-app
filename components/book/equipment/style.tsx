import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container:{
        display:"flex",
        gap:15,
    },
    equipmentContainer:{
        display:"flex",
        flexDirection:"row",
        gap:10,
    }
})